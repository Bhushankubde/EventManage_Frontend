feat: dynamic floating navbar and link visibility

- Redesigned the Navbar to be a sleek, floating, rounded container (max-width, custom margins, and padding) while preserving logo size.
- Reshuffled navbar links, moving the Cart button to the left of the authentication block.
- Implemented dynamic context-aware link visibility: links (e.g., Catalog, Admin, Offline Sales) disappear if they match the user's current route or the originally attempted route before an auth redirect.
- Ensured Login and Sign Up buttons mutually hide each other when viewing either authentication page.
