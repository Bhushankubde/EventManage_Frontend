feat: Admin Dashboard implementation and Authentication fixes

- Fixed a bug where admin logins were incorrectly redirected to the catalog due to case-sensitive role checking in the JWT parsing.
- Implemented InventoryManager and CategoryManager components for the Admin Dashboard to allow for easy inventory management.
- Integrated the frontend api.js service with endpoints for creating, updating, and deleting categories and items.
- Fixed a layout issue where the Add New Item modal was overlapped by the Navbar by utilizing a React Portal to render it outside the nested stacking context.
